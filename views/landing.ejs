




<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/landing-style.css">
</head>
<body class="bg-gray-100 text-gray-800">

    <!-- Header Section -->
    <header class="bg-white shadow-sm sticky top-0 z-50">
        <nav class="container mx-auto px-6 py-3 flex justify-between items-center">
            <div class="flex items-center space-x-4">
                <img src="/img/coat_of_arms.jpg" alt="Armoiries du Gabon" class="h-12 w-auto">
                <a href="/" class="text-2xl font-bold text-gray-800">e-Gouv <span class="text-green-600">Gabon</span></a>
            </div>
            <div id="desktop-menu" class="hidden md:flex items-center space-x-8">
                <a href="#categories" class="text-gray-600 hover:text-green-600 font-medium">Services</a>
                <a href="#demarches" class="text-gray-600 hover:text-green-600 font-medium">Démarches</a>
                <a href="#" class="bg-green-600 text-white font-semibold px-5 py-2 rounded-lg hover:bg-green-700 transition duration-300">
                    <i class="fas fa-user mr-2"></i>Mon Espace
                </a>
            </div>
            <button id="mobile-menu-button" class="md:hidden text-gray-700 focus:outline-none"><i class="fas fa-bars text-2xl"></i></button>
        </nav>
        <div id="mobile-menu" class="hidden md:hidden px-6 pb-4 space-y-2">
             <a href="#categories" class="block text-gray-600 hover:text-green-600 font-medium py-2">Services</a>
             <a href="#demarches" class="block text-gray-600 hover:text-green-600 font-medium py-2">Démarches</a>
             <a href="#" class="block bg-green-600 text-white font-semibold px-5 py-2 rounded-lg mt-2 text-center"><i class="fas fa-user mr-2"></i>Mon Espace</a>
        </div>
    </header>
    <main>
        <!-- ===================================================== -->
        <!-- ==              VUE PRINCIPALE                     == -->
        <!-- ===================================================== -->
        <div id="main-page" class="page-view">
            <!-- Hero Section -->
            <section class="relative h-[60vh] md:h-[70vh] flex items-center justify-center text-white text-center" style="background-image: url('https://images.unsplash.com/photo-1620573673190-2e4ab1a03974?q=80&w=2070&auto=format&fit=crop'); background-size: cover; background-position: center;">
                <div class="absolute inset-0 hero-gradient"></div>
                <div class="relative z-10 px-4">
                    <h1 class="text-4xl md:text-6xl font-extrabold mb-4 leading-tight">Le service public, simplifié pour vous.</h1>
                    <p class="text-lg md:text-xl max-w-3xl mx-auto mb-8 text-gray-200">Accédez à toutes vos démarches administratives en ligne, 24h/24 et 7j/7.</p>
                    <div class="bg-white rounded-full max-w-2xl mx-auto p-2 flex items-center shadow-2xl">
                        <i class="fas fa-search text-gray-400 mx-4"></i>
                        <input id="searchInput" type="text" placeholder="Rechercher un service..." class="w-full bg-transparent border-none focus:ring-0 text-gray-700">
                        <button id="searchButton" class="bg-blue-800 text-white font-semibold px-8 py-3 rounded-full hover:bg-blue-900 transition">Rechercher</button>
                    </div>
                </div>
            </section>

            
            <!-- Categories Section -->
            <section id="categories" class="py-20 bg-white">
                <div class="container mx-auto px-6">
                    <div class="text-center mb-12">
                        <h2 class="text-3xl md:text-4xl font-bold text-gray-800">Catégories de services</h2>
                        <p class="text-gray-600 mt-2 max-w-2xl mx-auto">Commencez par choisir une catégorie pour trouver votre démarche.</p>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                        <% for (const key in categories) { %>
                            <% const category = categories[key]; %>
                            <div onclick="showServices('<%= key %>')" class="group bg-gray-50 p-8 rounded-xl shadow-lg hover:shadow-2xl hover:-translate-y-2 transition-all duration-300 cursor-pointer text-center">
                                <div class="bg-<%= category.color %>-100 text-<%= category.color %>-600 inline-block p-5 rounded-full mb-4 group-hover:bg-<%= category.color %>-600 group-hover:text-white transition-colors">
                                    <i class="fas <%= category.icon %> fa-3x"></i>
                                </div>
                                <h3 class="text-xl font-bold mb-2"><%= category.title %></h3>
                                <p class="text-gray-600"><%= category.desc %></p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </section>
            
            <section id="demarches" class="py-20 bg-gray-100">
               <div class="container mx-auto px-6 text-center">
                    <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-12">Comment ça marche ?</h2>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
                        <div class="flex flex-col items-center">
                            <div class="bg-white rounded-full p-6 shadow-lg mb-4"><i class="fas fa-mouse-pointer text-green-600 text-4xl"></i></div>
                            <h3 class="text-xl font-bold mb-2">1. Choisissez un service</h3>
                            <p class="text-gray-600">Naviguez à travers les catégories et sélectionnez la démarche que vous souhaitez effectuer.</p>
                        </div>
                        <div class="flex flex-col items-center">
                            <div class="bg-white rounded-full p-6 shadow-lg mb-4"><i class="fab fa-whatsapp text-green-600 text-4xl"></i></div>
                            <h3 class="text-xl font-bold mb-2">2. Interagissez avec le bot</h3>
                            <p class="text-gray-600">Suivez les instructions de notre assistant WhatsApp pour fournir les informations nécessaires.</p>
                        </div>
                        <div class="flex flex-col items-center">
                            <div class="bg-white rounded-full p-6 shadow-lg mb-4"><i class="fas fa-check-circle text-green-600 text-4xl"></i></div>
                            <h3 class="text-xl font-bold mb-2">3. Obtenez votre document</h3>
                            <p class="text-gray-600">Recevez vos documents directement sur votre téléphone, prêts à être utilisés.</p>
                        </div>
                    </div>
                </div>
            </section>
        </div>

        <!-- ===================================================== -->
        <!-- ==           VUE DES SOUS-SERVICES (cachée)        == -->
        <!-- ===================================================== -->
        <div id="services-page" class="page-view hidden">
            <section class="py-20 bg-white">
                <div class="container mx-auto px-6">
                    <div class="flex items-center mb-12">
                        <button onclick="showMainPage()" class="text-gray-500 hover:text-green-600 text-2xl mr-4"><i class="fas fa-arrow-left"></i></button>
                        <div>
                            <h2 id="services-title" class="text-3xl md:text-4xl font-bold text-gray-800">Services</h2>
                            <p id="services-subtitle" class="text-gray-600 mt-1">Toutes les démarches disponibles pour cette catégorie.</p>
                        </div>
                    </div>
                    <div id="services-list" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                        <!-- Les cartes de services seront injectées ici par JavaScript -->
                    </div>
                </div>
            </section>
        </div>
    </main>
    
    <footer id="contact" class="bg-gray-800 text-white">
        <div class="container mx-auto px-6 py-12">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h3 class="text-lg font-bold mb-4">e-Gouv Gabon</h3>
                    <p class="text-gray-400">Votre portail unique pour des services publics accessibles, efficaces et transparents.</p>
                </div>
                <div>
                    <h3 class="text-lg font-bold mb-4">Liens Rapides</h3>
                    <ul>
                        <li class="mb-2"><a href="#" class="text-gray-400 hover:text-white">Accueil</a></li>
                        <li class="mb-2"><a href="#categories" class="text-gray-400 hover:text-white">Tous les services</a></li>
                        <li class="mb-2"><a href="#" class="text-gray-400 hover:text-white">FAQ</a></li>
                    </ul>
                </div>
                <div>
                    <h3 class="text-lg font-bold mb-4">Contact</h3>
                    <p class="text-gray-400"><i class="fas fa-phone mr-2"></i> +241 07 88 31 06</p>
                    <p class="text-gray-400"><i class="fas fa-envelope mr-2"></i> contact@service-public-gouv.ga</p>
                </div>
            </div>
            <div class="mt-8 border-t border-gray-700 pt-6 text-center text-gray-500">
                &copy; 2024 République Gabonaise. Tous droits réservés.
            </div>
        </div>
    </footer>

    <!-- ===================================================== -->
    <!-- ==              MODALS (cachées)                   == -->
    <!-- ===================================================== -->

    <!-- Choice Modal -->
    <div id="choiceModal" class="modal">
        <div class="modal-content">
            <header class="modal-header">
                <h2 id="choiceModalTitle">Lancer la démarche</h2>
                <button class="close-btn" onclick="closeChoiceModal()">&times;</button>
            </header>
            <div class="modal-body text-center">
                <p class="mb-6">Comment souhaitez-vous procéder ?</p>
                <div class="flex flex-col md:flex-row gap-4 justify-center">
                    <button id="whatsappBtn" class="w-full md:w-auto flex items-center justify-center gap-3 px-6 py-3 font-semibold text-white bg-green-500 rounded-lg hover:bg-green-600 transition-colors">
                        <i class="fab fa-whatsapp fa-lg"></i> Robot WhatsApp
                    </button>
                    <button id="webformBtn" class="w-full md:w-auto flex items-center justify-center gap-3 px-6 py-3 font-semibold text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 transition-colors">
                        <i class="fas fa-desktop"></i> Formulaire Web
                    </button>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Toast Notification Container -->
    <div id="toast-container"></div>


    <script>
        const mainPage = document.getElementById('main-page');
        const servicesPage = document.getElementById('services-page');
        const servicesTitle = document.getElementById('services-title');
        const servicesList = document.getElementById('services-list');
        const choiceModal = document.getElementById('choiceModal');
        const searchInput = document.getElementById('searchInput');
        const searchButton = document.getElementById('searchButton');

        // --- Base de données des services injectée par le serveur ---
        const servicesData = <%- JSON.stringify(servicesData) %>;

        // --- Le reste du script est identique ---
        let selectedServiceKey = '';
        let selectedServiceTitle = '';

        function showServices(categoryKey) {
            const categoryTitleEl = document.querySelector(`div[onclick="showServices('${categoryKey}')"] h3`);
            const categoryTitle = categoryTitleEl ? categoryTitleEl.textContent : "Services";
            servicesTitle.innerText = categoryTitle;

            servicesList.innerHTML = '';
            
            let servicesFound = false;
            for (const key in servicesData) {
                const service = servicesData[key];
                if (service.categoryKey === categoryKey) {
                    servicesFound = true;
                    const serviceCard = `
                        <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-xl hover:-translate-y-2 transition-all duration-300 flex flex-col items-start">
                            <div class="bg-${service.color}-100 text-${service.color}-600 p-4 rounded-full mb-4">
                                <i class="fas ${service.icon} fa-2x"></i>
                            </div>
                            <h3 class="text-lg font-bold mb-2">${service.title}</h3>
                            <p class="text-gray-600 text-sm mb-6 flex-grow">${service.desc}</p>
                            <button onclick="openChoiceModal('`+key.replace(/['"]/g, '\\$&')+`', '`+service.title.replace(/['"]/g, '\\$&')+`')" class="mt-auto bg-gray-800 text-white font-semibold px-5 py-2 rounded-lg hover:bg-gray-900 transition duration-300 self-end text-sm">
                                Accéder au service
                            </button>
                        </div>
                    `;
                    servicesList.innerHTML += serviceCard;
                }
            }

            if (!servicesFound) {
                 servicesList.innerHTML = `<p class="col-span-full text-center text-gray-500">Aucun service n'est encore défini pour cette catégorie.</p>`;
            }

            mainPage.classList.add('hidden');
            servicesPage.classList.remove('hidden');
            window.scrollTo(0, 0);
        }

        function handleSearch() {
            const searchTerm = searchInput.value.trim().toLowerCase();
            if (searchTerm.length < 2) {
                showToast("Veuillez entrer au moins 2 caractères pour rechercher.", 'warning');
                return;
            }

            const results = [];
            for (const key in servicesData) {
                const service = servicesData[key];
                const titleMatch = service.title.toLowerCase().includes(searchTerm);
                const descMatch = service.desc.toLowerCase().includes(searchTerm);
                if (titleMatch || descMatch) {
                    results.push({ key, ...service });
                }
            }
            displaySearchResults(results, searchTerm);
        }

        function displaySearchResults(results, term) {
            servicesTitle.innerText = `Résultats pour "${term}"`;
            servicesList.innerHTML = ''; 

            if (results.length === 0) {
                servicesList.innerHTML = `<p class="col-span-full text-center text-gray-500">Aucun service trouvé pour votre recherche.</p>`;
            } else {
                results.forEach(service => {
                    const serviceCard = `
                        <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-xl hover:-translate-y-2 transition-all duration-300 flex flex-col items-start">
                            <div class="bg-${service.color}-100 text-${service.color}-600 p-4 rounded-full mb-4">
                                <i class="fas ${service.icon} fa-2x"></i>
                            </div>
                            <h3 class="text-lg font-bold mb-2">${service.title}</h3>
                            <p class="text-gray-600 text-sm mb-6 flex-grow">${service.desc}</p>
                            <button onclick="openChoiceModal('`+service.key.replace(/['"]/g, '\\$&')+`', '`+service.title.replace(/['"]/g, '\\$&')+`')" class="mt-auto bg-gray-800 text-white font-semibold px-5 py-2 rounded-lg hover:bg-gray-900 transition duration-300 self-end text-sm">
                                Accéder au service
                            </button>
                        </div>
                    `;
                    servicesList.innerHTML += serviceCard;
                });
            }
            
            mainPage.classList.add('hidden');
            servicesPage.classList.remove('hidden');
            window.scrollTo(0, 0);
        }
        
        function showMainPage(event) {
            if (event) event.preventDefault();
            servicesPage.classList.add('hidden');
            mainPage.classList.remove('hidden');
            window.scrollTo(0, 0);
        }

        function openChoiceModal(serviceKey, serviceTitle) {
            selectedServiceKey = serviceKey; 
            selectedServiceTitle = serviceTitle;
            const modalTitle = document.getElementById('choiceModalTitle');
            modalTitle.innerText = `Lancer : ${serviceTitle}`;
            choiceModal.style.display = 'flex';
        }

        function closeChoiceModal() {
            choiceModal.style.display = 'none';
        }

        function showWebForm() {
            window.location.href = `${window.location.origin}/form/${selectedServiceKey}`;
        }
        
        function redirectToWhatsApp() {
            const phoneNumber = '24107883106'; 
            const message = `Bonjour, je souhaite commencer la démarche pour le service : ${selectedServiceTitle}`;
            const whatsappUrl = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;
            window.open(whatsappUrl, '_blank');
            closeChoiceModal();
        }

        function showToast(message, type = 'info') {
            const container = document.getElementById('toast-container');
            const toast = document.createElement('div');
            toast.className = `toast show toast-${type}`;
            let iconClass = type === 'warning' ? 'fa-exclamation-triangle' : 'fa-info-circle';
            toast.innerHTML = `<i class="fas ${iconClass}"></i> ${message}`;
            container.appendChild(toast);
            setTimeout(() => {
                toast.classList.remove('show');
                toast.classList.add('hide');
                toast.addEventListener('transitionend', () => toast.remove());
            }, 5000);
        }

        // --- Event Listeners ---
        searchButton.addEventListener('click', handleSearch);
        searchInput.addEventListener('keypress', (event) => {
            if (event.key === 'Enter') handleSearch();
        });

        document.getElementById('webformBtn').addEventListener('click', showWebForm);
        document.getElementById('whatsappBtn').addEventListener('click', redirectToWhatsApp);

        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        mobileMenuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });
    </script>
</body>
</html>
